# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

# Architecture

Always follow `PROJECT_STRUCTURE.md`. Keep routes thin, put UI in `src/features/`, use module stores (no Zustand / Redux / React Query), and match the naming and folder conventions in that doc.
